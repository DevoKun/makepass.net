
# https://circleci.com/docs/2.0/configuration-reference
version: 2.1


jobs:
  build:
    branches:
      only:
        - master
    environment:
      HELLO: WORLD
    docker:
      - image: devokun/gh-aws-node-python:latest
    steps:
      - checkout
      - run:
          name: step 1
          shell: /bin/bash
          command: |
            echo
            echo "pwd: $(pwd)"
            ls
            echo
            echo "done"
            echo

workflows:
  version: 2
    build:
      jobs:
        - build


