---

# https://circleci.com/docs/2.0/configuration-reference
version: 2.1

jobs:
  test:
    branches:
        only:
          - master
    environment:
      HELLO: WORLD
    steps:
      - checkout
      - run:
          shell: /bin/bash
          command: |+
            echo
            echo "pwd: $(pwd)"
            ls
            echo
            echo "done"
            echo


